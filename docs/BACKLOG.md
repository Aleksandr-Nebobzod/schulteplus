# BACKLOG

Задачи вне текущего трека (A/B в `API36_upgrade_plan.md`), приоритет по **MoSCoW**: M — Must, S — Should, C — Could, W — Won't.

| Приоритет | Задача | Описание / Что сделать | Оценка |
|---|---|---|---|
| S | **Обработчик ошибок подключения к Firebase** | Сейчас при кривом подключении к Firebase (нет сети, таймаут, сбой auth/синхронизации) приложение **молча падает** или молча не реагирует — пользователь не видит причину. Нужен обработчик: перехват исключений Firestore/auth, вывод понятного сообщения (snackbar/toast/dialog: «Нет соединения», «Не удалось синхронизировать» и т.п.) с возможностью повтора. Проверить все точки: `DataRepos` (запись в Firestore), `ExerciseRunner.complete()`, логин/синхронизация. Частично перекроется с B2 (переписывание auth) — согласовать при планировании B2 | 1 день |
| C | Вычистить фиктивный `google-services.json` из истории git | В истории (c159410) лежит пустой/фиктивный файл; репо публичный — настоящие креды туда не попадут, но для чистоты истории можно переписать через `git filter-repo` (форс-пуш, координация с удалённой веткой) | 0.5 дня |

## Лог изменений

| Дата | Изменение |
|---|---|
| 2026-08-03 | Создан бэклог (MoSCoW). Добавлена задача **S**: обработчик ошибок подключения к Firebase — понятное сообщение пользователю вместо молчаливого падения |
