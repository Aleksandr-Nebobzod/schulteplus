# BACKLOG

Задачи вне текущих треков (A/B — в `API36_upgrade_plan.md`; этапы перехода — в
`MODULAR_TRANSITION.md`), приоритет по **MoSCoW**: M — Must, S — Should, C — Could, W — Won't.
TaskID: `SP-{n}` (Schulte Plus). Решения по задачам — в `Decisions.md`.

| TaskID | Приоритет | Дата появления | Задача | Описание / Что сделать | Оценка |
|---|---|---|---|---|---|
| SP-01 | S | 2026-08-03 | **Обработчик ошибок подключения к Firebase** | Сейчас при кривом подключении к Firebase (нет сети, таймаут, сбой auth/синхронизации) приложение **молча падает** или молча не реагирует — пользователь не видит причину. Нужен обработчик: перехват исключений Firestore/auth, вывод понятного сообщения (snackbar/toast/dialog: «Нет соединения», «Не удалось синхронизировать» и т.п.) с возможностью повтора. Проверить все точки: `DataRepos` (запись в Firestore), `ExerciseRunner.complete()`, логин/синхронизация. Частично перекрывается с B2 (auth переписан на Compose — SP-03 учитывает ошибки входа) | 1 день |
| SP-02 | C | 2026-08-03 | Вычистить фиктивный `google-services.json` из истории git | В истории (c159410) лежит пустой/фиктивный файл; репо публичный — настоящие креды туда не попадут, но для чистоты истории можно переписать через `git filter-repo` (форс-пуш, координация с удалённой веткой) | 0.5 дня |
| SP-03 | S | 2026-08-16 | **Редизайн стартовых экранов (Auth + онбординг)** | ТЗ: `docs/TZ_StartScreens_Redesign.md` (статус: анализ, глубина — после ответов на вопросы раздела 6 ТЗ). Текущий Compose-флоу из B2 (Splash/Login/Signup): быстрый старт, инлайн-валидация, восстановление отложенных функций (reset/delete/resend), брендинг/тёмная тема, welcome-онбординг для новых пользователей (связь с `shownIntros`/TapTarget) | 3–5 дней |
| SP-04 | M | 2026-08-16 | **Упражнение «Мешанина» (gcb_schulte_4_mishmash)** | ТЗ: `docs/TZ_Mishmash.md`. Замощение поля 10×10 плитками 1–4 ячеек поверх поиска чисел 1–25; основа — `TileSquashPaving.java` (консольное демо). Пункт меню отключён (`FUNC_STATUS_PLANNED`, `coming_soon`). Сейчас выполняется на параллельном агенте | 2–3 дня |
| SP-05 | C | 2026-08-16 | **SDK 37 (compileSdk/targetSdk)** | После редизайна экранов на Compose (SP-03). compileSdk 37 уже используется локально; перевести официально compileSdk/targetSdk 37, проверить lifecycle 2.11.0 и прочие либы, требующие SDK 37 (см. A5-заметки), прогнать регресс | 1–2 дня |
| SP-06 | C | 2026-08-16 | **Цены упражнений и механика затрат псикойнов** | Решение пользователя: на стартовых экранах показываются статусы доступа (Schulte+Basics открыты, Spheres — «зарегистрируйтесь»), а не цены. Реальные цены (Schulte 4, Basics 50, Spheres 100 монет) и списание псикойнов — только с внедрением прокачки (дальнее будущее). Пока: зарегистрированные пользователи получают все упражнения без затрат псикойнов | 1–2 дня |

## Лог изменений

| Дата | Изменение |
|---|---|
| 2026-08-16 | Добавлена **SP-06 [C]**: цены упражнений и механика затрат псикойнов — при внедрении прокачки; стартовые экраны показывают статусы доступа (решение пользователя) |
| 2026-08-16 | В таблицу добавлены **TaskID (SP-01…SP-05)** и **дата появления**. Добавлены задачи: **SP-03 [S]** редизайн стартовых экранов (ТЗ `TZ_StartScreens_Redesign.md`), **SP-04 [M]** «Мешанина» (ТЗ `TZ_Mishmash.md`, на параллельном агенте), **SP-05 [C]** SDK 37 после редизайна. Создан `Decisions.md` (история решений) |
| 2026-08-03 | Создан бэклог (MoSCoW). Добавлена задача **S**: обработчик ошибок подключения к Firebase — понятное сообщение пользователю вместо молчаливого падения |
